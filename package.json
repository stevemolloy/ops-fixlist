{
  "name": "ops-fixlist",
  "version": "1.0.0",
  "description": "A fix-list for operations",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "devstart": "concurrently \"cd backend; npm run dev\" \"cd frontend; npm start\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.1",
    "import": "0.0.6",
    "react-router-dom": "^5.0.1"
  }
}
